.shell powershell

# Reload whkd configuration
# alt + o                 : taskkill /f /im whkd.exe && start /b whkd # if shell is cmd
alt + o                 : taskkill /f /im whkd.exe; Start-Process whkd -WindowStyle hidden # if shell is pwsh / powershell
alt + shift + o         : komorebic reload-configuration


# setting this up to match hammerspoon and hyprland
# $mainMod = SUPER_CTRL_ALT #mapped to caps w/ kmonad

# Apps
#bind = $mainMod, RETURN, exec, alacritty
#bind = $mainMod, A, exec, alacritty
#bind = $mainMod, K, exec, kitty
#bind = $mainMod, B, exec, google-chrome-stable
#bind = $mainMod, F, exec, thunar
#bind = $mainMod, T, exec, chromium --profile-directory=Default --app-id=cifhbcnohmdccbgoicgdjpfamggdegmo #Teams PWA
#bind = $mainMod, O, exec, chromium --profile-directory=Default --app-id=faolnafnngnfdaknnbpnkhgohbobgegn #Outlook PWA
#bind = $mainMod, M, exec, chromium --profile-directory=Default --app-id=hpfldicfbfomlpcikngkocigghgafkph #Google Messages PWA
#bind = $mainMod, C, exec, code #vs code

# Menus
#bind = $mainMod, R, exec, rofiWindow
#bind = $mainMod, SPACE, exec, rofiWindow
#bind = $mainMod, L, exec, wlogout 

# Commands
#bind = $mainMod, Q, killactive,
#bind = $mainMod, END, exit,
#bind = , Print, exec, grim -g "$(slurp)" - | wl-copy
#bind = SHIFT, Print, exec, grim -g "$(slurp)"

# Window Management
#bind = $mainMod, G, fullscreen,
#bind = $mainMod, home, pseudo, # dwindle
#bind = $mainMod, page_down, togglesplit, # dwindle
#bind = $mainMod, page_up, togglefloating, #dwindle
#bind = $mainMod, page_up, swapsplit, #dwindle

# App shortcuts - these require shell to be pwsh / powershell
# The apps will be focused if open, or launched if not open
ctrl + alt + win + b                 : Start-Process chrome
ctrl + alt + win + b                 : Start-Process vscode

# Workspaces
ctrl + alt + win + 1    : komorebic focus-workspace 0
ctrl + alt + win + 2    : komorebic focus-workspace 1
ctrl + alt + win + 3    : komorebic focus-workspace 2
ctrl + alt + win + 4    : komorebic focus-workspace 3
ctrl + alt + win + 5    : komorebic focus-workspace 4
ctrl + alt + win + 6    : komorebic focus-workspace 5
ctrl + alt + win + 7    : komorebic focus-workspace 6
ctrl + alt + win + 8    : komorebic focus-workspace 7


# Move windows across workspaces
ctrl + alt + win + shift + 1    : komorebic move-to-workspace 0
ctrl + alt + win + shift + 2    : komorebic move-to-workspace 1
ctrl + alt + win + shift + 3    : komorebic move-to-workspace 2
ctrl + alt + win + shift + 4    : komorebic move-to-workspace 3
ctrl + alt + win + shift + 5    : komorebic move-to-workspace 4
ctrl + alt + win + shift + 6    : komorebic move-to-workspace 5
ctrl + alt + win + shift + 7    : komorebic move-to-workspace 6
ctrl + alt + win + shift + 8    : komorebic move-to-workspace 7

# Focus windows
ctrl + left     : Switch-Desktop -Desktop ((Get-DesktopIndex) -1)
ctrl + right    : Switch-Desktop -Desktop ((Get-DesktopIndex) +1)

# Move windows
alt + shift + h         : komorebic move left
alt + shift + j         : komorebic move down
alt + shift + k         : komorebic move up
alt + shift + l         : komorebic move right
alt + shift + return    : komorebic promote

# Stack windows
alt + left              : komorebic stack left
alt + down              : komorebic stack down
alt + up                : komorebic stack up
alt + right             : komorebic stack right
alt + oem_1             : komorebic unstack # oem_1 is ;
alt + oem_4             : komorebic cycle-stack previous # oem_4 is [
alt + oem_6             : komorebic cycle-stack next # oem_6 is ]

# Resize
alt + oem_plus          : komorebic resize-axis horizontal increase
alt + oem_minus         : komorebic resize-axis horizontal decrease
alt + shift + oem_plus  : komorebic resize-axis vertical increase
alt + shift + oem_minus : komorebic resize-axis vertical decrease

# Manipulate windows
alt + t                 : komorebic toggle-float
alt + shift + f         : komorebic toggle-monocle

# Window manager options
alt + shift + r         : komorebic retile
alt + p                 : komorebic toggle-pause